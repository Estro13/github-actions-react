name: Playwright Tests
on:
  push:
    branches: [ main, master ]
jobs:
  three-test-jobs-in-one:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
      - uses: actions/checkout@v3
