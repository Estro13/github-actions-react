name: Test React App
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get the repository code
        # run: echo Ant text in console
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Test Our App
        run: npx playwright test